[package]
name = "arc"
version = "0.1.0"
authors = ["Alister Lee <alister@dev.shortepic.com>"]

[lib]
crate-type = ["staticlib"]

[dependencies]
rlibc = "1.0.0"
log = { version = "0.3.7", default-features = false }

[profile.dev]
opt-level = 1
panic = "abort"

[profile.release]
panic = "abort"
