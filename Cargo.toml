[package]
name = "arc"
version = "0.1.0"
authors = ["Alister Lee <alister@dev.shortepic.com>"]

[workspace]

[dependencies]
kernel = { path = "./kernel" }

[profile.dev]
opt-level = 1
panic = 'abort'

[profile.release]
panic = 'abort'
